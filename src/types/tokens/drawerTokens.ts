
export interface PipelineSolucoesDrawerTokens {
  
  background?: string,

  header?: {
    background?: string,
  }

  menuAvatar?: {
    background?: string,    
  }
  
  itemMenu?: {
    color?: string,
    colorSelected?: string,
  }

  tooltip?: {
    background?: string,
    color?: string,
  }

  loading?: {
    background?: string,
    spinnerSize?: number,
    color?: string,
  }
}